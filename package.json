{
  "name": "hydraallen-site-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only test harness for the personal site JS (pure lib + jsdom DOM tests).",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
