.DS_Store
**/.DS_Store
*.log
*.aux
*.bbl
*.out
*.gz

# Node dev dependencies (test harness only)
node_modules/

# Python test/cache artifacts
__pycache__/
*.pyc
.pytest_cache/

# Verification/tooling artifacts (Playwright smoke test output)
.playwright-mcp/
/index.png
/movies.png
/travel-lightbox.png

# Local working file for data/movies/movie_cleaner.py (not published to the site)
data/movies/backup.json

# Local-only CV source paths for tools/cv_sync_check.py (never commit local paths)
tools/cv_sources.local.json

# Local-only LifeChecklist snapshot path for tools/travel_sync_check.py
tools/travel_sources.local.json

# Local preview server state (manage-service.sh)
.preview/
